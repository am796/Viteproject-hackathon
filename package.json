{
  "name": "vite-dashboard-proto",
  "version": "1.0.0",
  "description": "Minimal dashboard prototype with auth, chart and table",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "nodemon --watch server --watch public --legacy-watch server/index.js",
    "dev:inspect": "nodemon --watch server --watch public --legacy-watch --inspect server/index.js"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "better-sqlite3": "^8.2.0",
    "mongoose": "^7.5.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
